import { useEffect, useState } from 'react';
import { useParams, Link, useNavigate } from 'react-router-dom';
import { Calendar, Clock, MapPin, Users, ArrowLeft, Ticket, CheckCircle2, AlertCircle, Heart, Star } from 'lucide-react';
import api from '../api/client';
import QrTicket from '../components/QrTicket';
import Spinner from '../components/Spinner';

export default function EventDetail() {
  const { id } = useParams(); const navigate = useNavigate(); const [event, setEvent] = useState(null); const [ticket, setTicket] = useState(null); const [error, setError] = useState(''); const [booking, setBooking] = useState(false); const [favorite, setFavorite] = useState(false); const [rating, setRating] = useState({ averageRating: 0, ratingCount: 0 }); const [myRating, setMyRating] = useState(0); const [ratingMessage, setRatingMessage] = useState(''); const [savingRating, setSavingRating] = useState(false);
  useEffect(() => { api.get('/events/' + id).then((res) => setEvent(res.data)).catch(() => setError('Could not load this event.')); api.get('/ratings/event/' + id).then((res) => setRating(res.data)).catch(() => {}); }, [id]);
  useEffect(() => { if (!localStorage.getItem('token')) return; api.get('/bookings/me').then((res) => { const existing = res.data.find((b) => b.event_id === Number(id) && b.status !== 'cancelled'); if (existing) setTicket({ ticketCode: existing.ticket_code, qrCode: existing.qr_code, status: existing.status }); }).catch(() => {}); api.get('/favorites/me').then((res) => setFavorite(res.data.includes(Number(id)))).catch(() => {}); api.get('/ratings/event/' + id + '/mine').then((res) => res.data && setMyRating(res.data.rating)).catch(() => {}); }, [id]);
  async function handleBook() { setError(''); if (!localStorage.getItem('token')) { navigate('/login'); return; } setBooking(true); try { const res = await api.post('/bookings', { eventId: Number(id) }); setTicket({ ticketCode: res.data.ticketCode, qrCode: res.data.qrCode, status: 'booked' }); setEvent((e) => e ? { ...e, seats_booked: e.seats_booked + 1 } : e); } catch (err) { if (err.response?.status === 401) { navigate('/login'); return; } setError(err.response?.data?.error || 'Booking failed. Please try again.'); } finally { setBooking(false); } }
  async function toggleFavorite() { if (!localStorage.getItem('token')) { navigate('/login'); return; } try { if (favorite) { await api.delete('/favorites/' + id); setFavorite(false); } else { await api.post('/favorites/' + id); setFavorite(true); } } catch (err) { setError(err.response?.data?.error || 'Could not update favorites.'); } }
  async function saveRating() { if (!myRating) return; setSavingRating(true); setRatingMessage(''); try { const res = await api.post('/ratings/event/' + id, { rating: myRating }); setRating(res.data); setRatingMessage('Thanks — your rating was saved.'); } catch (err) { setRatingMessage(err.response?.data?.error || 'Book this event before rating it.'); } finally { setSavingRating(false); } }
  if (error && !event) return <div className="max-w-2xl mx-auto px-4 py-20 text-center pt-24"><AlertCircle className="w-12 h-12 text-red-400 mx-auto mb-4" /><p className="text-slate-300 mb-4">{error}</p><Link to="/" className="text-astu-400 hover:text-astu-300 transition-colors">Back to events</Link></div>;
  if (!event) return <div className="pt-20"><Spinner label="Loading event…" /></div>;
  const seatsLeft = event.capacity - event.seats_booked; const isFull = seatsLeft <= 0; const eventDate = new Date(event.start_time);
  return <div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-24"><Link to="/" className="inline-flex items-center gap-2 text-slate-400 hover:text-astu-400 transition-colors mb-6 text-sm"><ArrowLeft className="w-4 h-4" /> Back to events</Link><div className="glass-card p-6 mb-6"><div className="flex items-start gap-4"><div className="flex flex-col items-center bg-slate-950/80 rounded-xl px-4 py-3 border border-white/10 flex-shrink-0"><span className="text-3xl font-bold text-astu-400 leading-none">{eventDate.getDate()}</span><span className="text-sm text-slate-300 uppercase">{eventDate.toLocaleDateString('en', { month: 'short' })}</span><span className="text-xs text-slate-500">{eventDate.getFullYear()}</span></div><div className="flex-1"><div className="flex items-start justify-between gap-3"><h1 className="font-display font-bold text-2xl text-slate-100 mb-2">{event.title}</h1><button type="button" aria-label={favorite ? 'Remove from favorites' : 'Add to favorites'} onClick={toggleFavorite} className="rounded-full border border-white/10 p-2 text-slate-300 hover:text-red-300"><Heart className={'w-5 h-5 ' + (favorite ? 'fill-red-400 text-red-400' : '')} /></button></div>{event.description && <p className="text-slate-400 leading-relaxed">{event.description}</p>}</div></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-3 mt-6"><div className="flex items-center gap-2 text-slate-300"><Clock className="w-5 h-5 text-astu-400" /> {eventDate.toLocaleString()}</div><div className="flex items-center gap-2 text-slate-300"><MapPin className="w-5 h-5 text-astu-400" /> {event.location || 'Location to be announced'}</div><div className="flex items-center gap-2 text-slate-300"><Users className="w-5 h-5 text-astu-400" /><span className={isFull ? 'text-red-400' : 'text-astuGreen-400'}>{isFull ? 'Fully booked' : seatsLeft + ' of ' + event.capacity + ' seats left'}</span></div><div className="flex items-center gap-2 text-amber-300"><Star className="w-5 h-5 fill-amber-300" /> {rating.ratingCount ? rating.averageRating + ' / 5 (' + rating.ratingCount + ')' : 'No ratings yet'}</div></div></div>{error && <div className="glass-card p-4 border-red-400/30 mb-4"><p className="text-red-400 text-sm flex items-center gap-2"><AlertCircle className="w-4 h-4" /> {error}</p></div>}{ticket ? <div><div className="flex items-center gap-2 text-astuGreen-400 mb-4"><CheckCircle2 className="w-5 h-5" /><span className="font-medium">You're booked for this event!</span></div><QrTicket qrCode={ticket.qrCode} ticketCode={ticket.ticketCode} status={ticket.status} eventTitle={event.title} /></div> : <button onClick={handleBook} disabled={isFull || booking} className="glow-btn w-full bg-gradient-to-r from-astu-500 to-astuGreen-500 text-white px-6 py-3 rounded-xl font-medium hover:shadow-lg hover:shadow-astu-500/30 transition-all disabled:opacity-50 disabled:cursor-not-allowed flex items-center justify-center gap-2">{booking ? 'Booking…' : isFull ? 'Fully Booked' : <><Ticket className="w-5 h-5" /> Book This Event</>}</button>}<div className="glass-card p-5 mt-6"><div className="flex items-center justify-between gap-3"><div><h2 className="font-display font-semibold text-slate-100">Rate this event</h2><p className="text-xs text-slate-500 mt-1">Only attendees with a booking can submit a rating.</p></div><div className="flex gap-1">{[1, 2, 3, 4, 5].map((value) => <button type="button" key={value} aria-label={'Rate ' + value + ' stars'} onClick={() => setMyRating(value)} className="p-1"><Star className={'w-6 h-6 ' + (value <= myRating ? 'fill-amber-300 text-amber-300' : 'text-slate-600')} /></button>)}</div></div><button type="button" onClick={saveRating} disabled={!myRating || savingRating || !ticket} className="mt-4 rounded-lg bg-white/10 px-4 py-2 text-sm text-slate-200 disabled:opacity-40">{savingRating ? 'Saving…' : 'Save rating'}</button>{ratingMessage && <p className="text-xs text-slate-400 mt-3">{ratingMessage}</p>}</div></div>;
}
